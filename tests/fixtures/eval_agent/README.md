# Eval Agent Fixtures

These fixtures evaluate whether the Eval Agent can judge engineering quality after deterministic verification.

Each case is copied into a model-specific eval working folder. The Eval Agent may inspect files and prior-stage artifacts, but must not edit anything.

