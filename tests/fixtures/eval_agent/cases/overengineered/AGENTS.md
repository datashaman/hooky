# Eval Fixture Project

This small CommonJS package implements a calculator addition feature.

Project preference: keep simple arithmetic behavior in `src/calculator.js` without additional framework layers.

