# Simple Calculator Agent Context

This fixture is a tiny CommonJS package used to evaluate the Verifier Agent.

The approved tests under `tests/generated/approved-simple/` are prior-stage artifacts and must not be modified.

